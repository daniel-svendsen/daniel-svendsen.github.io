pre-commit:
  commands:
    secrets:
      run: gitleaks git --staged --redact --verbose

pre-push:
  parallel: false
  commands:
    secrets-git:
      run: gitleaks git --log-opts="origin/main..HEAD" --redact --verbose
    typecheck:
      run: npx tsc --noEmit
